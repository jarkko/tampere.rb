TO BE DONE:

_ allow CRUD for admins only
- show who last edited an item and when
- add something FUN! Roster rankings? top attendees?
- mobile interface (append /m before routes? eg. host/m/events?)
- add separate event notes from event description, so that registered users can
  check afterwards what happened
- openid login
- add admin interface (user management)

FINISHED:

- add roles for registered users (member, admin)
- allow CRUD for registered users only

<div>

  <b>Sijanti:</b>
  <p>
  <%= @location.title %><br/>
  <%=h @location.address %> 
  <% unless @location.google_maps.blank? %>
    , <%= link_to 'kartta ->', @location.google_maps %>
  <% end %>
  </p>  
</div>

<p>
  <%= link_to 'Muokkaa', edit_location_path(@location) if logged_in? %> |
  <%= link_to 'Näytä sijainnit', locations_path %>
</p>

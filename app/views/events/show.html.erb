<div id="event_display">
  <% content_tag(:span, :class => "add_new") do %>
    <%= link_to('Lisää uusi', new_event_path) %> |
    <%= link_to 'Muokkaa', edit_event_path(@event) %> |
    <%= 
     button_to "Poista",
         { :action => "destroy", :id => @event.id }, 
           :confirm => "Haluatko varmasti poistaa tapahtuman?",
	   :method => :delete %>
  <% end if logged_in? %>

  <h2><%= fmt_date(@event.date) %></h2>
  
  <div id="program_details">  
    <div id="participants" class="tidbit">
      Osallistujat (<%= @event.registered_count %>):
      <%= show_users(@event.attendees) %>
    </div>
    
    Paikka: <%= link_to @event.location.title, @event.location if @event.location %><br/>
    Aika: 18.30 - 21.00

    <h3>Ohjelma:</h3>
    <div id="program">
      <%= format_event(@event) %>
    </div>

  </div>
</div>

<p class="clearing" id="back_link">
  <%= link_to '&larr; Takaisin tapahtumiin', events_path %>
</p>
